export function WhatThisIs() {
  return (
    <section id="overview" className="scroll-mt-8 border-t border-border py-16">
      <div className="mx-auto max-w-5xl px-5 sm:px-8">
        <h2 className="text-2xl font-semibold tracking-tight">What this is</h2>
        <p className="mt-5 max-w-2xl text-base leading-relaxed text-muted">
          A layer that takes the outward per-call USDC payments agents make and
          lands them on a company&apos;s accounting and spend-control rails —
          not replacing internal bookkeeping, but merging x402 outward spend
          into it.
        </p>
      </div>
    </section>
  );
}
